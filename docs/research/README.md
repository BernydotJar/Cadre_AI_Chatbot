# Cadre public-source inventory

This directory is a research/provenance layer, not a runtime scraper or vector store. The shipped assistant knowledge remains the reviewed, typed configuration in `src/config/cadre.ts`.

`cadre-public-sources-20260909.json` records the official pages inspected for the 2026-09-09 refresh and the specific public claims considered during curation. A claim enters runtime behavior only after explicit review plus routing/safety tests.
