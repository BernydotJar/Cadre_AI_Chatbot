# Owner authorization — cinematic proactive Donna increment

Date: 2026-09-09 UTC

The owner authorizes a forward-only premium product increment that keeps Cadre AI factual authority and safety boundaries intact while making the public experience feel more like an AI-native Cadre property and less like a static side-by-side demo.

Authorized outcomes:

- Recompose the public page around Cadre's own editorial/business language and current color system, with the chatbot presented as an embedded/floating Donna experience rather than a permanent half-screen panel.
- Keep the existing local ambient video and add only one or two restrained interaction effects. Inspiration may be drawn from Motion, SceneAI, thinking-orb patterns, and glare/microinteraction references, but implementation must remain original and dependency-light.
- Give Donna a more visible, animated presence: a compact launcher/avatar, a short proactive hint/fact, and a distinct shaping/thinking state that invites interaction without blocking content or fabricating capability.
- Add useful first-turn hints so visitors know what to ask; keep them grounded in the existing Cadre knowledge/configuration.
- Improve boundary copy for pricing/unknown/account questions so it is concise, empathetic, lightly playful, and commercially aware while still refusing to invent pricing, promises, security claims, or private facts.
- Promote the verified public Cadre message about tracking tools, agents, training, and results, with only approved contact routing where no public portal URL is verified.
- Add an on-page "How Donna works" / engineering-trust explanation that makes architecture and boundaries legible without exposing private evaluation context.
- Simplify the favicon/product mark.
- Update persona/product configuration and durable Markdown so the product behavior is explicit in source, not implied only by UI copy.
- Use the existing Graph Harness SDLC lifecycle. Preserve prior FAIL→repair evidence and keep all existing release/public guarantees fail-closed.
- Run product tests only after the complete producer implementation is assembled; then execute Critic/Red Team → Fixer → Independent Verifier → Release Gate → persistent evidence.

Explicit non-goals:

- No new database, auth, CRM, analytics, vector store, GraphRAG, autonomous actions, external page scraping, account access, booking integration, or unbounded model-generated sales behavior.
- No external video/image copying into the product and no replacement Vercel project.
- No hidden claims that Cadre endorses this independent implementation beyond facts already verified from official public sources.
