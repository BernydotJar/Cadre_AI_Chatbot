# Premium experience checkpoint — Donna

Updated: 2026-09-10 UTC. The append-only premium Graph ledger is authoritative for this increment.

- Graph: `progress/premium-graph.project.json` + `progress/premium-graph.events.jsonl`.
- `PX2-donna-premium-identity` — **DONE** with `design-review=PASS`, `verification=PASS`, `integration-proof=PASS`. The first authenticated Claude Code critic requested changes; repair + fresh critic + separate verifier closed them.
- `PX3-ambient-media` — **DONE** with all three gates PASS. Retained repair loops cover perceptibility, mobile Pause overlap, responsive coverage, local pinned media, reduced-motion poster fallback, and Pause/Play behavior.
- `PX3B-premium-shell-refinement` — **DONE** with all three gates PASS. One restrained editorial Donna mark replaces the rejected synthetic AI-badge/ring language; independent critic and verifier evidence is retained.
- `PX4-contextual-donna` — **DONE (revision 1)** with all three gates PASS after critic/fixer/verifier repair of sub-12px disclosure/privacy/context copy. Current adapter remains pathname/hash-only presentation context with no page-content/account/storage reads.
- `PX5-premium-release` — **DONE (revision 4)** with `design-review=PASS`, `verification=PASS`, and `integration-proof=PASS`. Earlier failures are retained rather than erased: stale release documentation; a synthetic panel-frame verifier race; then a real public ambient Pause/autoplay cold-load failure after revision 3 deployment. Revision 4 makes visible application state authoritative for Pause/Play intent, adds a deterministic cold-load regression, and fixes `release:gate` so `failure.recorded` + `node.invalidated` revoke stale DONE/PASS projections.
- Revision-4 detached verification (`db72cc1...`) passes Git integrity, lockfile install, typecheck/lint, **288/288 Vitest**, production build, **58/58 Playwright**, **72/72 extension tests**, a fresh **24/24 synthetic-browser** run, Graph validation, and tracked-source drift checks. Existing-project Vercel production prebuild also passes on the repaired line.
- Repaired revision-4 source `8ae8a3a1b0e53100218930aa893eed90a8592562` is now deployed to the existing production project as `dpl_EECF4m6NbwdN73j6Z2JxSfVpWbP7`. Anonymous health/page/icon + Donna behavior smoke and the full **58/58** public Playwright matrix PASS. The earlier revision-3 57/58 failure remains retained separately.
- `npm run release:gate` passes after the revision-4 repair/gates. Audited publication and GitHub CI run `34405396973` PASS on `8ae8a3a`; the existing-project manual deployment and anonymous **58/58** public verification also PASS. Automated GitHub CD remains a separate blocked workstream because its `production` environment lacks the Vercel binding.
- `PX6-cinematic-proactive-donna` — **REVISION 11 / RUNNING**. Revision 10 previously earned `design-review=PASS`, `verification=PASS`, and `integration-proof=PASS`; its detached clean verifier passed Git integrity, lockfile install, typecheck/lint, **301/301 Vitest**, production build, and **68/68 Playwright**. Closure source `69c590b` then passed GitHub CI run `34442075458`, while deploy run `34442247941` failed closed only on the absent GitHub production Vercel binding. Before public PX6 equivalence, a 2026-09-10 source audit found the public **Get Your AI Results** CTA now points to `https://portal.gocadre.ai/ai-maturity-index`; the Graph invalidated revision 10 and opened revision 11 to update that bounded source/provenance/link contract; the assembled repair is now RUNNING for independent source critique. The public destination is treated as an AI Maturity Index entry point, **not** a verified private client-login portal. Actual Claude Code 2.1.266 produced the bounded source-refresh draft; coordinator review narrowed domain trust and corrected provenance. No r11 runtime PASS is claimed yet.

- Git history remains forward-only. Actual Claude Code review/verifier executions, session-limit failures, Granite timeouts/inconsistent outputs, and accepted/rejected findings remain distinct evidence classes; author/committer metadata is not treated as model provenance.

Next safe action: commit the assembled revision-11 source repair and Claude Producer evidence, run an independent read-only Claude Code critic, repair only if that critic identifies a real defect, and then run the complete detached verifier once at the end. Only fresh r11 gates may reopen publication/deployment. PX5 remains the rollback baseline until PX6 public equivalence passes.
