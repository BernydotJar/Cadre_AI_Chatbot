# PX4 fixer — contextual Donna readability repair

Date: 2026-09-09 UTC.

The independent Claude Code critic on producer snapshot `ec1002b39077420eb8ad4b0d906ba66d852baaee` returned `CHANGES_REQUESTED`. The reproduced blocking defect was presentation-only: user-visible mode/privacy/boundary copy in the local extension panel used 7–11px text, below the repository's 12px readability floor. The critic also noted wording that could imply live page-content verification even though PX4 is URL-enum presentation context only.

## Repair

- Raised all user-visible extension text previously below 12px to at least 12px, including the local-preview disclosure, approved-knowledge kicker, page-context label, context suggestion, status/privacy copy, message role label, and launcher disclosure.
- Removed the mobile 7px disclosure override.
- Reworded the welcome/status language from "verified context" to "approved Cadre knowledge" so route context cannot be mistaken for page-content verification.
- Kept the contextual contract unchanged: exact approved `pathname/hash` -> fixed enum; no DOM/page-text/forms/cookies/storage/account reads; no arbitrary destination; one fixed suggested grounded question; server knowledge/policy remain authoritative.
- Added a synthetic-browser computed-style regression asserting the critical mode/privacy/boundary/status surfaces are all at least 12px.

## Fixer verification

- `node extension/build.mjs` — PASS; 15 generated local files.
- `npm exec -- vitest run --config extension/vitest.config.ts` — 72/72 PASS.
- `node extension/tests/browser-mock.mjs px4-fix-contextual-donna-20260909` — 24/24 PASS; zero real API/site requests.
- `EXTENSION_ACTUAL_SITE=1 node extension/tests/installed-site.mjs px4-fix-actual-site-20260909` — 16 PASS + one informational site-overflow observation; exactly one live API request.
- `npm run typecheck` — PASS.
- `npm run lint` — PASS.
- Source scan of `extension/src/**` finds no user-visible `font-size` below 12px and no recruiting/candidate presentation label.

This report is fixer evidence, not independent verification or release approval.
