## Verdict: PASS (source-level only)

## Findings
None. No defect found in `extension/build.mjs`, `extension/tests/build.test.ts`, `extension/config.json`, `src/config/cadre.ts`, `src/config/types.ts`, or the manifest-generation path that would block revision 12 or cause the next `node extension/build.mjs` run to fail on the current canonical config.

### Point-by-point

1. **Normalization** — `extension/build.mjs:40-44` parses `client.officialDomain` and each `client.additionalOfficialDomains` entry with `new URL(domain).hostname` before comparison, exactly mirroring `src/config/types.ts:130-133` (`validateClientConfig`). The representation bug the coordinator caught in the first Claude Fixer draft (comparing `url.hostname` to raw `https://...` strings) is not present in the committed source.
2. **Suffix matching** — `isApprovedHost` (`build.mjs:45`) uses `hostname === host || hostname.endsWith(`.${host}`)`, identical to the server-side check. Verified by hand this rejects lookalikes (`xcadre.ai`, `notgocadre.ai`, `cadre.ai.attacker.com`) and only matches exact hosts or true dot-delimited subdomains. `additionalOfficialDomains` in the active config is scoped to the exact observed `https://portal.gocadre.ai` (`src/config/cadre.ts:36`), not the `gocadre.ai` apex, so no sibling-domain widening exists in current data.
3. **Fail-closed** — `build.mjs:37-51` throws on non-string/empty `officialDomain`, non-array/empty-string `additionalOfficialDomains`, non-`https:` protocol, embedded username/password, non-default port, missing/empty link label, and non-allowlisted hostname. Malformed URL strings throw via the `new URL()` constructor uncaught, which still crashes the build (non-zero exit) rather than silently succeeding — acceptable fail-closed behavior, unchanged from prior style.
4. **No authority drift** — `extension/config.json` (site origins, API endpoint) is unchanged and still enforced verbatim at `build.mjs:10-16`; manifest `content_scripts`/`web_accessible_resources` matches (`build.mjs:80,88-90`), `host_permissions` (`build.mjs:86`), and CSP `connect-src`/`frame-ancestors` (`build.mjs:90`) are all still derived only from the fixed `config.siteOrigins`/`endpoint`, not from `approvedHosts`. Grepped `extension/src/*` for `cadre.ai`/`gocadre` — no other hardcoded site logic exists outside `build.mjs`/`config.json`.
5. **Generic vs. ClientConfig-consistent** — `build.mjs` never imports/executes `src/config/cadre.ts`; it reads it via the TypeScript AST literal-extraction helper (`literal()`, `build.mjs:20-35`) and applies host-approval logic structurally identical to `validateClientConfig`. Only `client.contact` + `client.knowledge[].approvedLinks` feed the extension's `links`/`presentation` (`build.mjs:46,52`); `client.publicHighlights` links (e.g. the `track-ai-results` highlight at `cadre.ai:68-73`) are not rendered by the extension panel and are therefore correctly out of this validator's scope — their host safety is still guaranteed separately by `validateClientConfig`, which does cover `publicHighlights` (`src/config/types.ts:137`).
6. **Test coverage** — `extension/tests/build.test.ts:45-52` is a new, non-trivial regression: it independently locates the real `https://portal.gocadre.ai/ai-maturity-index` link from the imported canonical `cadre` config (not a hardcoded duplicate), asserts it is present in generated `PRESENTATION.links`, and re-asserts `manifest.content_scripts` stays exactly the two Cadre origins. All prior parity assertions (fixed API host, no `permissions`/`externally_connectable`, exact `web_accessible_resources`, secret/eval/storage bans, file count) are untouched — no weakening.
7. **Documentation honesty** — `extension/README.md:22` accurately describes the new validation scope ("affects only links rendered inside the panel and does not widen... activation sites, API destination, or browser permissions"). `progress/checkpoint.md`, `plan.md:261-267`, and both evidence files (`evidence/claude-code/px6-r12-extension-link-fixer-20260910.md`, `evidence/premium-release/px6-r12-extension-link-repair-20260910.md`) consistently and explicitly state no revision-12 test/build/browser/deployment PASS is claimed yet, and correctly attribute the coordinator's correction of the Claude Fixer's draft. Ledger tail (`progress/premium-graph.events.jsonl:216-217`) matches: sequence 217 is an `evidence.recorded`/`implementation_summary` event, a pattern already used identically at sequences 194 and 208 for prior unverified handoffs — `"result":"PASS"` there means "evidence write succeeded," not "tests passed," consistent with established ledger convention, not a new ambiguity.
8. **Would the next build still fail?** — Manually checked every `url:` in `src/config/cadre.ts` (contact + all 6 `knowledge[].approvedLinks`, deduplicated): all resolve to hostname `cadre.ai` or `portal.gocadre.ai`, both directly in `approvedHosts`. `client.knowledge.length === 6` holds. No link would trip the validator on the current canonical config.

## Not verified (explicitly out of scope / not run)
- No `npm ci`, `typecheck`, `lint`, `vitest`, production `build`, `node extension/build.mjs`, extension vitest, Playwright, browser, or release-gate command was executed — this is a static source review only, per the read-only constraint.
- Did not diff `8206cd9`→`b8e0acf` via `git show`/`git diff` (Bash forbidden); conclusions are based on reading current committed file state plus the written evidence/ledger descriptions of what changed, not a literal commit-by-commit diff.
- Did not re-inspect `extension/src/content-script.ts`, `panel.ts`, `service-worker.ts`, `contracts.ts`, `bridge.ts` line-by-line (only grepped for `cadre.ai`/`gocadre` references) since the handoff states they are unchanged and out of this repair's boundary.
- Did not re-run or re-derive the revision-11 `tests/config/*`/`tests/core/policy.test.ts`/`tests/api/chat-route.test.ts` regressions; out of scope for the r12 extension-link delta.
- Per instructions, this PASS does not itself close the gate — a fresh detached runtime verifier (git integrity, `npm ci`, typecheck, lint, full Vitest, production build, extension build, extension vitest, Playwright) must still run and pass before revision 12 can move past `running`/`review`.
